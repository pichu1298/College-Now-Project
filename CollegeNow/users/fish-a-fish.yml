info:
  name: fish-a-fish
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/users/:id/fish"
  params:
    - name: id
      value: 69ca8e83da8adb6d25eefedf
      type: path
  auth: inherit

runtime:
  variables:
    - name: base_url
      value: http://localhost:3000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: fish-a-fish
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/users/:id/fish"
  params:
    - name: id
      value: 69cd301f2febed762503caa6
      type: path
  auth: inherit

runtime:
  variables:
    - name: base_url
      value: http://localhost:3000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: add-friend
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url}}/users/:id/friends"
  params:
    - name: id
      value: 69cbdf4fba0954ed1f6c14d2
      type: path
  body:
    type: json
    data: |-
      {
        "friendId": "69cbddc12b742596edbfc04c"
      }
  auth: inherit

runtime:
  variables:
    - name: base_url
      value: http://localhost:3000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

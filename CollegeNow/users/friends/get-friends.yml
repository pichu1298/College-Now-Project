info:
  name: get-friends
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/users/:id/friends"
  params:
    - name: id
      value: 69cbdf53ba0954ed1f6c14d4
      type: path
  auth: inherit

runtime:
  variables:
    - name: base_url
      value: http://localhost:3000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: get-itemDex
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/users/:id/itemDex"
  params:
    - name: id
      value: 69cd29ef72b05d290848da0a
      type: path
  auth: inherit

runtime:
  variables:
    - name: base_url
      value: http://localhost:3000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: get-all-users
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/users"
  body:
    type: json
    data: |-
      {
          "username": "Admin",
          "password": "password"
      }
  auth: inherit

runtime:
  variables:
    - name: base_url
      value: http://localhost:3000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

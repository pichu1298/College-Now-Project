info:
  name: get-all-users
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/users"
  body:
    type: json
    data: |-
      {
          "username": "HI",
          "password": "password"
      }
  auth: inherit

runtime:
  variables:
    - name: base_url
      value: http://localhost:3000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

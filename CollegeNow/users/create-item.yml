info:
  name: create-item
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/users/:id/items"
  params:
    - name: id
      value: 69dd034da3fe37e4d7a61724
      type: path
  body:
    type: json
    data: |-
      {
          "name": "Minor Heh Potion",
          "description": "Boosts your strength slightly for a short time.",
          "picture": "https://example.com/images/minor_strength.png",
          "strength_required": 0,
          "rarity": "common",
          "buffs": [
            { "stat": "strength", "value": 5 }
          ]
        }
  auth: inherit

runtime:
  variables:
    - name: base_url
      value: http://localhost:3000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

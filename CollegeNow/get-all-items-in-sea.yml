info:
  name: get-all-items-in-sea
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/items"
  auth: inherit

runtime:
  variables:
    - name: base_url
      value: http://localhost:3000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

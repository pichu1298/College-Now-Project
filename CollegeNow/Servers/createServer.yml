info:
  name: createServer
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}"
  auth: inherit

runtime:
  variables:
    - name: base_url
      value: http://localhost:3000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
